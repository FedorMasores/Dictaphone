<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/recordingFragment">

    <fragment
        android:id="@+id/recordingFragment"
        android:name="com.example.dictaphone.fragments.RecordingFragment"
        android:label="fragment_recording"
        tools:layout="@layout/fragment_recording" >
        <action
            android:id="@+id/action_recordingFragment_to_listenerFragment"
            app:destination="@id/listenerFragment" />
    </fragment>
    <fragment
        android:id="@+id/listenerFragment"
        android:name="com.example.dictaphone.fragments.ListenerFragment"
        android:label="fragment_listener"
        tools:layout="@layout/fragment_listener" />
</navigation>